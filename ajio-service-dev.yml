spring:
  application:
    name: ajio-user

  datasource:
    url: jdbc:mysql://host.docker.internal:3306/ajio_user_dev
    username: root
    password: Sharankumar@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://host.docker.internal:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

# Optional: server port for your service
server:
  port: 8081
